const input=document.querySelector('#file-input'),preview=document.querySelector('#preview'),prompt=document.querySelector('#prompt'),button=document.querySelector('#scan-button'),statusText=document.querySelector('#status'),zone=document.querySelector('#drop-zone'),uploadView=document.querySelector('#upload-view'),resultView=document.querySelector('#result-view'),resultImage=document.querySelector('#result-image');
let selectedFile=null,previewUrl=null;
function choose(file){if(!file)return;selectedFile=file;if(previewUrl)URL.revokeObjectURL(previewUrl);previewUrl=URL.createObjectURL(file);preview.src=previewUrl;preview.style.display='block';prompt.style.display='none';button.disabled=false;statusText.textContent='';}
input.addEventListener('change',()=>choose(input.files[0]));
['dragenter','dragover'].forEach(e=>zone.addEventListener(e,event=>{event.preventDefault();zone.classList.add('drag')}));
['dragleave','drop'].forEach(e=>zone.addEventListener(e,event=>{event.preventDefault();zone.classList.remove('drag')}));
zone.addEventListener('drop',event=>choose(event.dataTransfer.files[0]));
button.addEventListener('click',async()=>{if(!selectedFile)return;button.disabled=true;button.firstChild.textContent='Analyzing… ';statusText.textContent='';const data=new FormData();data.append('file',selectedFile);try{const response=await fetch('/api/predict',{method:'POST',body:data});const body=await response.json();if(!response.ok)throw new Error(body.detail||'Analysis failed.');resultImage.src=previewUrl;document.querySelector('#disease').textContent=body.disease;document.querySelector('#remedy').textContent=body.remedy;document.querySelector('#demo-badge').hidden=!body.demo;document.querySelector('#disclaimer').textContent=body.disclaimer;uploadView.hidden=true;resultView.hidden=false;requestAnimationFrame(()=>document.querySelector('.result-card').scrollIntoView({behavior:'smooth',block:'start'}));}catch(error){statusText.textContent=error.message;}finally{button.disabled=false;button.firstChild.textContent='Analyze leaf ';}});
function resetScanner(){resultView.hidden=true;uploadView.hidden=false;selectedFile=null;input.value='';preview.removeAttribute('src');preview.style.display='none';prompt.style.display='flex';button.disabled=true;statusText.textContent='';window.scrollTo({top:0,behavior:'smooth'});}
document.querySelector('#scan-again').addEventListener('click',resetScanner);
document.querySelector('#scan-again-top').addEventListener('click',resetScanner);
